---
title: Stream de la Conversación
description: Escucha el hilo en tiempo real vía Server-Sent Events (SSE).
badge:
  text: Experimental
  variant: caution
---

import { Code } from '@astrojs/starlight/components';
import ApiCodeTabs from '../../../components/ApiCodeTabs.astro';
import { BASE_API_IAFLOWS } from '../../../constants';

### Endpoint

<Code lang="http" code={`GET ${BASE_API_IAFLOWS}/api/workflows/:id/flow-chat/:cid/stream`} />

Abrilo **apenas tenés el conversationId**, antes de mandar el primer mensaje: el saludo del flujo ya está corriendo.

### Parámetros de Ruta

| Parámetro | Tipo | Requerido | Descripción | Ejemplo |
|-----------|------|-----------|-------------|---------|
| `:id` | string | Sí | ID del workflow | `00000000-0000-0000-0000-000000000000` |
| `:cid` | string | Sí | `conversationId` devuelto al [abrir la conversación](/flow-chat/abrir-conversacion/) | `00000000-0000-0000-0000-000000000000` |

### Headers

| Header | Valor |
|--------|-------|
| Accept | text/event-stream |
| Authorization | Bearer `{{token}}` |

### Ejemplos

<ApiCodeTabs>
<Fragment slot="curl">

<Code
  lang="bash"
  code={`curl -N -X GET ${BASE_API_IAFLOWS}/api/workflows/00000000-0000-0000-0000-000000000000/flow-chat/00000000-0000-0000-0000-000000000000/stream \\
    -H "Accept: text/event-stream" \\
    -H "Authorization: Bearer tu_token_aqui"`}
/>

</Fragment>
<Fragment slot="javascript">

<Code
  lang="javascript"
  code={`const response = await fetch(
    \`${BASE_API_IAFLOWS}/api/workflows/\${workflowId}/flow-chat/\${conversationId}/stream\`,
    {
      headers: {
        'Accept': 'text/event-stream',
        'Authorization': \`Bearer \${token}\`
      }
    }
  );

  const reader = response.body.pipeThrough(new TextDecoderStream()).getReader();
  let buffer = '';

  while (true) {
    const { value, done } = await reader.read();
    if (done) break;
    buffer += value;

    const blocks = buffer.split('\\n\\n');
    buffer = blocks.pop();

    for (const block of blocks) {
      // el heartbeat llega como "event: heartbeat" con data vacía
      if (block.startsWith('event: heartbeat')) continue;
      const data = block.split('\\n').find((l) => l.startsWith('data: '));
      if (data) render(JSON.parse(data.slice(6)));
    }
  }`}
/>

</Fragment>
<Fragment slot="python">

<Code
  lang="python"
  code={`import json
  import requests

  workflow_id = "00000000-0000-0000-0000-000000000000"
  conversation_id = "00000000-0000-0000-0000-000000000000"
  url = f"${BASE_API_IAFLOWS}/api/workflows/{workflow_id}/flow-chat/{conversation_id}/stream"
  headers = {
      "Accept": "text/event-stream",
      "Authorization": "Bearer tu_token_aqui"
  }

  with requests.get(url, headers=headers, stream=True) as response:
      for line in response.iter_lines(decode_unicode=True):
          if line and line.startswith("data: "):
              payload = line[6:]
              if payload:  # el heartbeat manda data vacía
                  render(json.loads(payload))`}
/>

</Fragment>
<Fragment slot="php">

<Code
  lang="php"
  code={`<?php

  $url = "${BASE_API_IAFLOWS}/api/workflows/00000000-0000-0000-0000-000000000000/flow-chat/00000000-0000-0000-0000-000000000000/stream";
  $headers = ["Accept: text/event-stream", "Authorization: Bearer tu_token_aqui"];

  $ch = curl_init($url);
  curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
  curl_setopt($ch, CURLOPT_WRITEFUNCTION, function ($ch, $chunk) {
      foreach (explode("\\n", $chunk) as $line) {
          if (str_starts_with($line, "data: ") && strlen($line) > 6) {
              render(json_decode(substr($line, 6), true));
          }
      }
      return strlen($chunk);
  });
  curl_exec($ch);
  curl_close($ch);`}
/>

</Fragment>
<Fragment slot="java">

<Code
  lang="java"
  code={`import java.net.URI;
  import java.net.http.HttpClient;
  import java.net.http.HttpRequest;
  import java.net.http.HttpResponse;

  var client = HttpClient.newHttpClient();
  var request = HttpRequest.newBuilder()
      .uri(URI.create("${BASE_API_IAFLOWS}/api/workflows/00000000-0000-0000-0000-000000000000/flow-chat/00000000-0000-0000-0000-000000000000/stream"))
      .header("Accept", "text/event-stream")
      .header("Authorization", "Bearer tu_token_aqui")
      .GET()
      .build();

  var response = client.send(request, HttpResponse.BodyHandlers.ofLines());
  response.body()
      .filter(line -> line.startsWith("data: ") && line.length() > 6)
      .forEach(line -> render(line.substring(6)));`}
/>

</Fragment>
</ApiCodeTabs>

:::caution
`EventSource` del browser **no permite mandar headers**, así que no sirve para un endpoint con `Authorization`. Los ejemplos usan `fetch` con lectura de stream. Si tu integración necesita `EventSource`, el token tiene que viajar de otra forma — confirmalo con backend.
:::

### Formato de cada Emisión

Cada emisión es el hilo **entero** — estado completo, no incrementos: se reemplaza, no se deduplica.

```json
{
  "type": "conversation",
  "conversationId": "uuid",
  "turn": 3,
  "messages": [
    { "kind": "bot", "id": "uuid", "text": "¡Hola! ¿En qué te ayudo?", "at": "..." },
    { "kind": "user", "id": "uuid", "text": "Mirá esta factura", "at": "...", "attachments": ["uuid.png"] },
    { "kind": "note", "id": "uuid", "text": "La vuelta falló", "at": "...", "tone": "error" },
    { "kind": "reasoning", "id": "uuid", "text": "Pensando...", "at": "..." }
  ],
  "ask": {
    "executionId": "uuid",
    "nodeId": "nodo-3",
    "prompt": "¿Cuál es tu email?",
    "validation": "email",
    "attempt": 1,
    "attemptsLeft": 2
  },
  "working": "Pensando · Clasificador…",
  "workingNodeId": "nodo-3",
  "ended": null
}
```

| Campo | Tipo | Descripción |
|-------|------|-------------|
| `messages[].kind` | string | `bot`, `user`, `note` o `reasoning` (solo modelos de reasoning) |
| `messages[].attachments` | string[] | IDs de adjuntos, **no URLs**. La URL la arma el cliente |
| `ask` | object \| null | Pregunta abierta esperando respuesta |
| `working` | string \| null | Etiqueta del nodo que está corriendo |
| `ended` | string \| null | `completed`, `failed` o `cancelled` cuando la vuelta termina |

### Cómo Renderizarlo

| Estado | Qué hacer |
|--------|-----------|
| `ask ≠ null` | Mostrar el campo de respuesta con el tipo que dice `validation` |
| `working ≠ null` | Indicador con **esa** etiqueta (no inventes un "escribiendo…": no hay tokens) |
| `ended ≠ null` | La vuelta terminó. **No cierres el EventSource**: escribir de nuevo arranca otra vuelta por esta misma conexión |

### Heartbeat

Cada 15s llega `event: heartbeat` con data vacía. Tiene nombre de evento propio, así que **no cae en `onmessage`** — un `JSON.parse('')` ahí te rompería el chat. Para detectar una conexión colgada (EventSource no ve un socket half-open), escuchalo con `addEventListener` y reconectá si pasan más de 35s sin ningún evento.

### Vueltas Trabadas

Si un nodo se cuelga sin fallar (~4 min sin reportar nada), el backend corta la vuelta solo: llega una nota con tono error, `working: null` y `ended: "failed"`. El cliente no mide nada.

Exentos: `delay` (su silencio es legítimo) y cualquier nodo mientras `ask` esté abierto (esperar a una persona no es colgarse).

### Errores

| Código | Cuerpo | Posible Causa |
|--------|--------|---------------|
| 404 | `conversation_not_found` | Conversación inexistente |
