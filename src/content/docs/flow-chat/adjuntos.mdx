---
title: Adjuntos
description: Sirve los bytes de un adjunto del hilo de conversación.
badge:
  text: Experimental
  variant: caution
---

import { Code } from '@astrojs/starlight/components';
import ApiCodeTabs from '../../../components/ApiCodeTabs.astro';
import { BASE_API_IAFLOWS } from '../../../constants';

### Endpoint

<Code lang="http" code={`GET ${BASE_API_IAFLOWS}/api/workflows/:id/chat/:cid/attachments/:attachmentId`} />

:::caution
Es `/chat/`, **no** `/flow-chat/`. Mismo storage, otra ruta (`routes/attachments.ts`). El `:cid` es el mismo conversationId del flow-chat.
:::

El `attachmentId` sale de `messages[].attachments` del [stream](/flow-chat/stream/).

### Parámetros de Ruta

| Parámetro | Tipo | Requerido | Descripción | Ejemplo |
|-----------|------|-----------|-------------|---------|
| `:id` | string | Sí | ID del workflow | `00000000-0000-0000-0000-000000000000` |
| `:cid` | string | Sí | `conversationId` del hilo | `00000000-0000-0000-0000-000000000000` |
| `:attachmentId` | string | Sí | ID del adjunto, con extensión | `00000000-0000-0000-0000-000000000000.png` |

### Headers

| Header | Valor |
|--------|-------|
| Authorization | Bearer `{{token}}` |

### Ejemplos

<ApiCodeTabs>
<Fragment slot="curl">

<Code
  lang="bash"
  code={`curl -X GET ${BASE_API_IAFLOWS}/api/workflows/00000000-0000-0000-0000-000000000000/chat/00000000-0000-0000-0000-000000000000/attachments/00000000-0000-0000-0000-000000000000.png \\
    -H "Authorization: Bearer tu_token_aqui" \\
    --output factura.png`}
/>

</Fragment>
<Fragment slot="javascript">

<Code
  lang="javascript"
  code={`const response = await fetch(
    \`${BASE_API_IAFLOWS}/api/workflows/\${workflowId}/chat/\${conversationId}/attachments/\${attachmentId}\`,
    { headers: { 'Authorization': \`Bearer \${token}\` } }
  );
  const blob = await response.blob();
  const url = URL.createObjectURL(blob);`}
/>

</Fragment>
<Fragment slot="python">

<Code
  lang="python"
  code={`import requests

  workflow_id = "00000000-0000-0000-0000-000000000000"
  conversation_id = "00000000-0000-0000-0000-000000000000"
  attachment_id = "00000000-0000-0000-0000-000000000000.png"
  url = f"${BASE_API_IAFLOWS}/api/workflows/{workflow_id}/chat/{conversation_id}/attachments/{attachment_id}"
  headers = {"Authorization": "Bearer tu_token_aqui"}

  response = requests.get(url, headers=headers)
  with open("factura.png", "wb") as f:
      f.write(response.content)`}
/>

</Fragment>
<Fragment slot="php">

<Code
  lang="php"
  code={`<?php

  $url = "${BASE_API_IAFLOWS}/api/workflows/00000000-0000-0000-0000-000000000000/chat/00000000-0000-0000-0000-000000000000/attachments/00000000-0000-0000-0000-000000000000.png";
  $headers = ["Authorization: Bearer tu_token_aqui"];

  $ch = curl_init($url);
  curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
  curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
  $response = curl_exec($ch);
  curl_close($ch);
  file_put_contents("factura.png", $response);`}
/>

</Fragment>
<Fragment slot="java">

<Code
  lang="java"
  code={`import java.net.URI;
  import java.net.http.HttpClient;
  import java.net.http.HttpRequest;
  import java.net.http.HttpResponse;
  import java.nio.file.Path;

  var client = HttpClient.newHttpClient();
  var request = HttpRequest.newBuilder()
      .uri(URI.create("${BASE_API_IAFLOWS}/api/workflows/00000000-0000-0000-0000-000000000000/chat/00000000-0000-0000-0000-000000000000/attachments/00000000-0000-0000-0000-000000000000.png"))
      .header("Authorization", "Bearer tu_token_aqui")
      .GET()
      .build();

  var response = client.send(request, HttpResponse.BodyHandlers.ofInputStream());
  java.nio.file.Files.copy(response.body(), Path.of("factura.png"));`}
/>

</Fragment>
</ApiCodeTabs>

### Respuesta Exitosa (200)

Devuelve los **bytes** del archivo con el `Content-Type` de la whitelist —nunca el que declaró el cliente— y el header `X-Content-Type-Options: nosniff`. Las dos cosas van juntas: es lo que deja inerte un archivo que mintió sobre su tipo.

### Errores

| Código | Cuerpo | Posible Causa |
|--------|--------|---------------|
| 404 | `not_found` | Adjunto inexistente o ya borrado |
| 400 | `invalid_id` | ID con formato inválido |
| 415 | `unsupported_type` | Tipo fuera de la whitelist |
| 500 | `read_failed` | Falla al leer del storage |

:::tip
Los adjuntos se borran definitivamente al [cerrar la conversación](/flow-chat/cerrar-conversacion/).
:::
