---
title: Parámetros de una Plantilla
description: Obtén la estructura de parámetros esperada por una plantilla antes de enviarla.
---

import { Code } from '@astrojs/starlight/components';
import ApiCodeTabs from '../../../components/ApiCodeTabs.astro';
import { BASE_API_URL } from '../../../constants';

### Endpoint

<Code lang="http" code={`GET ${BASE_API_URL}/api/v1/whatsapp/template-params?name={{name}}`} />

### Parámetros de Query

| Parámetro | Tipo | Requerido | Descripción | Ejemplo |
|-----------|------|-----------|-------------|---------|
| `name` | string | Sí | Nombre de la plantilla | `salio_ruta` |

### Headers

| Header | Valor |
|--------|-------|
| Authorization | Bearer `{{token}}` |

### Ejemplos

<ApiCodeTabs>
<Fragment slot="curl">

<Code
  lang="bash"
  code={`curl -X GET "${BASE_API_URL}/api/v1/whatsapp/template-params?name=salio_ruta" \\
    -H "Authorization: Bearer tu_token_aqui"`}
/>

</Fragment>
<Fragment slot="javascript">

<Code
  lang="javascript"
  code={`const response = await fetch(
    \`${BASE_API_URL}/api/v1/whatsapp/template-params?name=\${templateName}\`,
    { headers: { 'Authorization': \`Bearer \${token}\` } }
  );
  const data = await response.json();`}
/>

</Fragment>
<Fragment slot="python">

<Code
  lang="python"
  code={`import requests

  url = "${BASE_API_URL}/api/v1/whatsapp/template-params"
  params = {"name": "salio_ruta"}
  headers = {"Authorization": "Bearer tu_token_aqui"}

  response = requests.get(url, params=params, headers=headers)
  data = response.json()`}
/>

</Fragment>
<Fragment slot="php">

<Code
  lang="php"
  code={`<?php

  $url = "${BASE_API_URL}/api/v1/whatsapp/template-params?name=salio_ruta";
  $headers = ["Authorization: Bearer tu_token_aqui"];

  $ch = curl_init($url);
  curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
  curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
  $response = curl_exec($ch);
  curl_close($ch);
  $data = json_decode($response, true);`}
/>

</Fragment>
<Fragment slot="java">

<Code
  lang="java"
  code={`import java.net.URI;
  import java.net.http.HttpClient;
  import java.net.http.HttpRequest;
  import java.net.http.HttpResponse;

  var client = HttpClient.newHttpClient();
  var request = HttpRequest.newBuilder()
      .uri(URI.create("${BASE_API_URL}/api/v1/whatsapp/template-params?name=salio_ruta"))
      .header("Authorization", "Bearer tu_token_aqui")
      .GET()
      .build();

  var response = client.send(request, HttpResponse.BodyHandlers.ofString());
  var data = response.body();`}
/>

</Fragment>
</ApiCodeTabs>

### Respuesta Exitosa (200)

```json
{
  "code": 200,
  "data": {
    "template_name": "salio_ruta",
    "template_body": [
      {
        "parameters": [
          { "parameter_name": "cliente_name", "type": "text", "text": "" },
          { "parameter_name": "shipper_name", "type": "text", "text": "" },
          { "parameter_name": "tracking", "type": "text", "text": "" },
          { "parameter_name": "dir_delivery", "type": "text", "text": "" },
          { "parameter_name": "ciudad", "type": "text", "text": "" }
        ],
        "type": "body"
      },
      {
        "index": 0,
        "parameters": [{ "type": "text", "text": "" }],
        "sub_type": "url",
        "type": "button"
      }
    ]
  },
  "errors": {},
  "messages": "OK",
  "status": 200,
  "success": true
}
```

| Campo | Tipo | Descripción |
|-------|------|-------------|
| `template_name` | string | Nombre de la plantilla |
| `template_body[].type` | string | Tipo: `body` o `button` |
| `template_body[].parameters` | array | Lista de parámetros esperados |
| `template_body[].parameters[].parameter_name` | string | Nombre del parámetro |
| `template_body[].sub_type` | string | Subtipo del botón (`url`) |

### Errores

```json
{
  "code": 200,
  "data": null,
  "errors": {},
  "messages": "Opps plantilla no encontrada",
  "status": 400,
  "success": true
}
```

| Código | Mensaje | Posible Causa |
|--------|---------|---------------|
| 400 | Opps plantilla no encontrada | La plantilla no existe en el canal |
| 401 | No autorizado | Token expirado o inválido |
