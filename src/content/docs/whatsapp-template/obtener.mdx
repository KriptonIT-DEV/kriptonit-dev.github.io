---
title: Obtener Plantilla por Nombre
description: Consulta los detalles completos de una plantilla específica.
---

import { Code } from '@astrojs/starlight/components';
import ApiCodeTabs from '../../../components/ApiCodeTabs.astro';
import { BASE_API_URL } from '../../../constants';

### Endpoint

<Code lang="http" code={`GET ${BASE_API_URL}/api/v1/whatsapp/template?name={{name}}`} />

### Parámetros de Query

| Parámetro | Tipo | Requerido | Descripción | Ejemplo |
|-----------|------|-----------|-------------|---------|
| `name` | string | Sí | Nombre de la plantilla | `salio_ruta` |

### Headers

| Header | Valor |
|--------|-------|
| Authorization | Bearer `{{token}}` |

### Ejemplos

<ApiCodeTabs>
<Fragment slot="curl">

<Code
  lang="bash"
  code={`curl -X GET "${BASE_API_URL}/api/v1/whatsapp/template?name=salio_ruta" \\
    -H "Authorization: Bearer tu_token_aqui"`}
/>

</Fragment>
<Fragment slot="javascript">

<Code
  lang="javascript"
  code={`const response = await fetch(
    \`${BASE_API_URL}/api/v1/whatsapp/template?name=\${templateName}\`,
    { headers: { 'Authorization': \`Bearer \${token}\` } }
  );
  const data = await response.json();`}
/>

</Fragment>
<Fragment slot="python">

<Code
  lang="python"
  code={`import requests

  url = "${BASE_API_URL}/api/v1/whatsapp/template"
  params = {"name": "salio_ruta"}
  headers = {"Authorization": "Bearer tu_token_aqui"}

  response = requests.get(url, params=params, headers=headers)
  data = response.json()`}
/>

</Fragment>
<Fragment slot="php">

<Code
  lang="php"
  code={`<?php

  $url = "${BASE_API_URL}/api/v1/whatsapp/template?name=salio_ruta";
  $headers = ["Authorization: Bearer tu_token_aqui"];

  $ch = curl_init($url);
  curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
  curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
  $response = curl_exec($ch);
  curl_close($ch);
  $data = json_decode($response, true);`}
/>

</Fragment>
<Fragment slot="java">

<Code
  lang="java"
  code={`import java.net.URI;
  import java.net.http.HttpClient;
  import java.net.http.HttpRequest;
  import java.net.http.HttpResponse;

  var client = HttpClient.newHttpClient();
  var request = HttpRequest.newBuilder()
      .uri(URI.create("${BASE_API_URL}/api/v1/whatsapp/template?name=salio_ruta"))
      .header("Authorization", "Bearer tu_token_aqui")
      .GET()
      .build();

  var response = client.send(request, HttpResponse.BodyHandlers.ofString());
  var data = response.body();`}
/>

</Fragment>
</ApiCodeTabs>

### Respuesta Exitosa (200)

```json
{
  "code": 200,
  "data": {
    "category": "UTILITY",
    "components": [
      {
        "buttons": [
          {
            "text": "Rastrea en nuestra web",
            "type": "URL",
            "url": "https://www.urbano.com.pe/tracking/?guides={{1}}"
          }
        ],
        "text": "Hola {{cliente_name}}, somos urbano...",
        "type": "BODY"
      }
    ],
    "language": "es",
    "name": "salio_ruta",
    "status": "APPROVED"
  },
  "errors": {},
  "messages": "OK",
  "status": 200,
  "success": true
}
```

### Estructura de Componentes

| Campo | Tipo | Descripción |
|-------|------|-------------|
| `components[].type` | string | Tipo: `BODY`, `HEADER`, `BUTTONS` |
| `components[].text` | string | Texto con placeholders `{{variable}}` |
| `components[].buttons` | array | Botones asociados |
| `components[].example` | object | Ejemplos de valores para los parámetros |

### Errores

Los errores de autenticación son comunes a toda la API — ver [Convenciones](/guides/convenciones/).
