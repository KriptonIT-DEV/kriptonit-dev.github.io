---
title: Obtener Cliente
description: Consulta la información de un cliente según el canal e identificador.
---

import { Code } from '@astrojs/starlight/components';
import ApiCodeTabs from '../../../components/ApiCodeTabs.astro';
import { BASE_API_URL } from '../../../constants';

### Endpoint

<Code lang="http" code={`GET ${BASE_API_URL}/api/external/customer?channel={{channel}}&identifier={{identifier}}`} />

### Parámetros de Query

| Parámetro | Tipo | Requerido | Descripción | Ejemplo |
|-----------|------|-----------|-------------|---------|
| `channel` | string | Sí | Canal de comunicación | `whatsapp` |
| `identifier` | string | Sí | Identificador del cliente | `999888777` |

### Headers

| Header | Valor |
|--------|-------|
| Authorization | Bearer `{{token}}` |

### Ejemplos

<ApiCodeTabs>
<Fragment slot="curl">

<Code
  lang="bash"
  code={`curl -X GET "${BASE_API_URL}/api/external/customer?channel=whatsapp&identifier=999888777" \\
  -H "Authorization: Bearer tu_token_aqui"`}
/>

</Fragment>
<Fragment slot="javascript">

<Code
  lang="javascript"
  code={`const response = await fetch(
  \`${BASE_API_URL}/api/external/customer?channel=whatsapp&identifier=999888777\`,
  { headers: { 'Authorization': \`Bearer \${token}\` } }
);
const data = await response.json();`}
/>

</Fragment>
<Fragment slot="python">

<Code
  lang="python"
  code={`import requests

url = "${BASE_API_URL}/api/external/customer"
params = {"channel": "whatsapp", "identifier": "999888777"}
headers = {"Authorization": "Bearer tu_token_aqui"}

response = requests.get(url, params=params, headers=headers)
data = response.json()`}
/>

</Fragment>
<Fragment slot="php">

<Code
  lang="php"
  code={`<?php

$url = "${BASE_API_URL}/api/external/customer?channel=whatsapp&identifier=999888777";
$headers = ["Authorization: Bearer tu_token_aqui"];

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
$response = curl_exec($ch);
curl_close($ch);
$data = json_decode($response, true);`}
/>

</Fragment>
<Fragment slot="java">

<Code
  lang="java"
  code={`import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

var client = HttpClient.newHttpClient();
var request = HttpRequest.newBuilder()
    .uri(URI.create("${BASE_API_URL}/api/external/customer?channel=whatsapp&identifier=999888777"))
    .header("Authorization", "Bearer tu_token_aqui")
    .GET()
    .build();

var response = client.send(request, HttpResponse.BodyHandlers.ofString());
var data = response.body();`}
/>

</Fragment>
</ApiCodeTabs>

### Respuesta Exitosa (200)

```json
{
  "success": true,
  "data": {
    "cus_id": 1,
    "cus_nombre": "Juan CC",
    "cus_apellido": "",
    "tel_id": 1,
    "mail_id": 0,
    "mail": "",
    "tel_numero": "999888777",
    "rrss_id": "",
    "cia_id": 1,
    "last_ticket_id": null,
    "canal_id": 1
  },
  "code": 0,
  "status": 200,
  "messages": ""
}
```

| Campo | Tipo | Descripción |
|-------|------|-------------|
| `cus_id` | number | ID único del cliente |
| `cus_nombre` | string | Nombre del cliente |
| `cus_apellido` | string | Apellido del cliente |
| `tel_id` | number | ID del teléfono registrado |
| `tel_numero` | string | Número de teléfono |
| `mail` | string | Correo electrónico |
| `cia_id` | number | ID de la empresa |
| `last_ticket_id` | number \| null | ID del último ticket |
| `canal_id` | number | ID del canal |

### Errores

```json
{
  "success": false,
  "data": { "canal_id": 1 },
  "code": -1,
  "status": 400,
  "messages": "Error: No hay datos.",
  "errors": { "code": -1, "status": 400 }
}
```

| Código | Mensaje | Posible Causa |
|--------|---------|---------------|
| 400 | Error: No hay datos. | Cliente o canal no encontrado |

Los errores de autenticación son comunes a toda la API — ver [Convenciones](/guides/convenciones/).

:::tip
Usá el `cus_id` de la respuesta como `customer_id` en el siguiente endpoint para obtener los tickets.
:::
