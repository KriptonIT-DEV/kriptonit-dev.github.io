---
title: Obtener Medios
description: Descarga archivos multimedia asociados a un ticket.
---

import { Aside } from '@astrojs/starlight/components';
import ApiCodeTabs from '../../../components/ApiCodeTabs.astro';

### Endpoint 

```
GET /api/external/ticket/medias?channel_id={{channel_id}}&mime_id={{mime_id}}
```

### Parámetros de Query

| Parámetro | Tipo | Requerido | Descripción | Ejemplo |
|-----------|------|-----------|-------------|---------|
| `channel_id` | string | Sí | ID del canal | `1` |
| `mime_id` | string | Sí | ID del archivo multimedia | `546097577936591` |

### Headers

| Header | Valor |
|--------|-------|
| Authorization | Bearer `{{token}}` |

### Ejemplos

<ApiCodeTabs>
<Fragment slot="curl">

```bash
curl -X GET "https://devkapi.konnect-360.pe/api/external/ticket/medias?channel_id=1&mime_id=546097577936591" \
  -H "Authorization: Bearer tu_token_aqui" \
  --output archivo.gif
```

</Fragment>
<Fragment slot="javascript">

```javascript
const response = await fetch(
  `https://devkapi.konnect-360.pe/api/external/ticket/medias?channel_id=1&mime_id=546097577936591`,
  { headers: { 'Authorization': `Bearer ${token}` } }
);
const blob = await response.blob();
```

</Fragment>
<Fragment slot="python">

```python
import requests

url = "https://devkapi.konnect-360.pe/api/external/ticket/medias"
params = {"channel_id": "1", "mime_id": "546097577936591"}
headers = {"Authorization": "Bearer tu_token_aqui"}

response = requests.get(url, params=params, headers=headers)
with open("archivo.gif", "wb") as f:
    f.write(response.content)
```

</Fragment>
<Fragment slot="php">

```php
<?php

$url = "https://devkapi.konnect-360.pe/api/external/ticket/medias?channel_id=1&mime_id=546097577936591";
$headers = ["Authorization: Bearer tu_token_aqui"];

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
$response = curl_exec($ch);
curl_close($ch);
file_put_contents("archivo.gif", $response);
```

</Fragment>
<Fragment slot="java">

```java
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.nio.file.Path;

var client = HttpClient.newHttpClient();
var request = HttpRequest.newBuilder()
    .uri(URI.create("https://devkapi.konnect-360.pe/api/external/ticket/medias?channel_id=1&mime_id=546097577936591"))
    .header("Authorization", "Bearer tu_token_aqui")
    .GET()
    .build();

var response = client.send(request, HttpResponse.BodyHandlers.ofInputStream());
java.nio.file.Files.copy(response.body(), Path.of("archivo.gif"));
```

</Fragment>
</ApiCodeTabs>

### Headers de Respuesta

| Header | Descripción |
|--------|-------------|
| Content-Type | `application/octet-stream` (binario) |
| X-File-Type | Tipo MIME original (ej: `image/gif`) |

<Aside type="caution">
  La respuesta es el **binario del archivo**, no un JSON. Usá `response.blob()` en JS o `--output` en cURL.
</Aside>
